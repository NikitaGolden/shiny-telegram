<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TempPhoneHub - Buy Verification Numbers</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="style.css">
</head>
<body class="bg-gradient-to-br from-purple-900 to-indigo-900 min-h-screen">
    
    <!-- Login/Register Modal -->
    <div id="authModal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 hidden">
        <div class="bg-white p-8 rounded-2xl max-w-md w-full mx-4">
            <h2 class="text-2xl font-bold text-gray-800 mb-6">Login / Register</h2>
            <input id="username" type="text" placeholder="Username" class="w-full p-3 border rounded-lg mb-4">
            <input id="password" type="password" placeholder="Password" class="w-full p-3 border rounded-lg mb-6">
            <button onclick="authUser()" class="w-full bg-gradient-to-r from-purple-500 to-indigo-500 text-white py-3 rounded-lg font-bold">ENTER</button>
        </div>
    </div>

    <!-- Main Dashboard -->
    <div class="container mx-auto px-4 py-12 max-w-2xl">
        <div id="welcome" class="text-center mb-12">
            <h1 class="text-4xl font-bold text-white mb-4">🚀 TempPhoneHub</h1>
            <p class="text-xl text-gray-300 mb-8">Buy US/Canada/UK numbers for Telegram/WhatsApp/Signal</p>
            <button onclick="showAuth()" class="bg-gradient-to-r from-green-500 to-green-600 text-white px-8 py-4 rounded-xl font-bold text-lg">GET STARTED</button>
        </div>

        <div id="dashboard" class="hidden bg-white/20 backdrop-blur-xl rounded-3xl p-8">
            <div class="flex justify-between items-center mb-8">
                <h2 class="text-3xl font-bold text-white">Buy Number</h2>
                <div class="text-right">
                    <div class="text-2xl font-bold text-green-400">$2.50 USDT</div>
                    <div id="balance" class="text-sm text-gray-300">Balance: $0.00</div>
                </div>
            </div>

            <!-- Selection -->
            <div class="grid grid-cols-2 md:grid-cols-3 gap-4 mb-8">
                <select id="country" class="p-4 bg-white/30 backdrop-blur rounded-xl text-white">
                    <option value="us">🇺🇸 US (+1)</option>
                    <option value="ca">🇨🇦 Canada (+1)</option>
                    <option value="uk">🇬🇧 UK (+44)</option>
                </select>
                <select id="service" class="p-4 bg-white/30 backdrop-blur rounded-xl text-white">
                    <option value="telegram">💬 Telegram</option>
                    <option value="whatsapp">📱 WhatsApp</option>
                    <option value="signal">🔒 Signal</option>
                </select>
            </div>

            <!-- Payment -->
            <div id="paymentSection" class="hidden">
                <select id="paymentMethod" class="w-full p-4 bg-white/30 backdrop-blur rounded-xl text-white mb-6">
                    <option value="erc20">USDT ERC20</option>
                    <option value="trc20">USDT TRC20</option>
                    <option value="bep20">USDT BEP20</option>
                    <option value="solana">Solana</option>
                </select>

                <div id="walletInfo" class="bg-white/20 p-6 rounded-2xl mb-6 hidden">
                    <div class="text-center mb-4">
                        <div id="walletAddress" class="font-mono bg-black/50 p-3 rounded-lg text-sm break-all"></div>
                        <img id="qrCode" class="mx-auto mt-4" width="150">
                    </div>
                    <input id="txHash" placeholder="Enter Transaction Hash" class="w-full p-4 bg-white/50 rounded-xl">
                    <button onclick="verifyPayment()" class="w-full bg-gradient-to-r from-orange-500 to-red-500 text-white py-4 rounded-xl font-bold mt-4">VERIFY PAYMENT (60s)</button>
                </div>

                <button onclick="showPayment()" class="w-full bg-gradient-to-r from-purple-500 to-indigo-500 text-white py-4 rounded-xl font-bold text-lg">PROCEED TO PAYMENT</button>
            </div>

            <!-- Number Delivery -->
            <div id="numberSection" class="hidden">
                <div class="bg-green-500/20 border-2 border-green-500 p-6 rounded-2xl text-center">
                    <h3 class="text-2xl font-bold text-green-400 mb-4">✅ PAYMENT CONFIRMED!</h3>
                    <div class="bg-black/50 p-4 rounded-xl mb-4">
                        <div id="phoneNumber" class="text-3xl font-mono text-white"></div>
                    </div>
                    <div id="smsCodes" class="space-y-2">
                        <!-- SMS codes appear here -->
                    </div>
                    <button onclick="refreshSMS()" class="mt-4 bg-blue-500 text-white px-6 py-2 rounded-lg">🔄 Refresh SMS</button>
                </div>
            </div>
        </div>
    </div>

    <script src="script.js"></script>
</body>
</html>
